{
  "name": "@liberation-os/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo lint apps/worker",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo no tests for worker"
  },
  "dependencies": {
    "bullmq": "^5.44.0",
    "ioredis": "^5.6.1",
    "tsx": "^4.19.3",
    "@liberation-os/workflow-engine": "workspace:*",
    "@liberation-os/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
{
  "name": "@liberation-os/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo lint packages/types",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo no tests for types"
  }
}
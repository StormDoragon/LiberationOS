{
  "name": "@liberation-os/analytics",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo lint packages/analytics",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo no tests for analytics"
  }
}